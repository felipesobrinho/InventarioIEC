# name: Database Migrations

# on:
#   pull_request:
#     paths:
#       - 'prisma/schema.prisma'
#       - 'supabase/migrations/**'
#       - 'scripts/check-migrations.js'
#       - 'package.json'
#       - 'package-lock.json'
#   push:
#     branches: [main, dev, dev-v1.0, prod]
#     paths:
#       - 'prisma/schema.prisma'
#       - 'supabase/migrations/**'
#       - 'scripts/check-migrations.js'
#       - 'package.json'
#       - 'package-lock.json'
#   schedule:
#     - cron: '0 10 * * 1'

# permissions:
#   contents: read

# jobs:
#   validate-migrations:
#     runs-on: ubuntu-latest

#     steps:
#       - uses: actions/checkout@v4

#       - name: Setup Node.js
#         uses: actions/setup-node@v4
#         with:
#           node-version: '20.x'
#           cache: 'npm'

#       - name: Install dependencies
#         run: npm ci

#       - name: Check migration files
#         run: npm run db:check-migrations

#       - name: Validate Prisma schema
#         run: npx prisma validate

#       - name: Verify baseline SQL can be generated
#         run: npx prisma migrate diff --from-empty --to-schema-datamodel prisma/schema.prisma --script > /tmp/generated-baseline.sql
